[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scv-contracts"
version = "0.1.0"
description = "Shared event and configuration contracts between the Smart Café Vision backend and AI worker."
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
include = ["scv_contracts*"]
