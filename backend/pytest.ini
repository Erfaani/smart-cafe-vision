[pytest]
DJANGO_SETTINGS_MODULE = config.settings.test
python_files = test_*.py
addopts = -q --strict-markers --disable-warnings
testpaths = apps
markers =
    integration: needs a real PostgreSQL or Redis instance
